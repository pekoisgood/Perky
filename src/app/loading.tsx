import React from "react";

const Loading = () => {
  return (
    <div className="animate-wiggle w-fit text-[30px] font-bold fixed top-[calc(50%-60px)] right-[-50%] translate-x-[50%] translate-y-[50%]">
      Loading...
    </div>
  );
};

export default Loading;
